# Dipendenze
node_modules/

# Segreti locali (la config di sviluppo è in .env.dev, committato)
.env
.env.local
.env.*.local
.env.production

# Log e output
logs/
*.log
npm-debug.log*

# Editor / IDE / strumenti
.claude/
CLAUDE.local.md
.vscode/
.idea/
*.swp

# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Cache e temporanei
.cache/
tmp/
*.tmp

# Simulazione di una serata (npm run db:sim non esiste: v. CLAUDE.local.md).
# Rende TUTTI gli utenti account di prova: resta solo in locale.
db/sim-serata.sql

# Dump del database locale
*.sql.gz
*.dump
